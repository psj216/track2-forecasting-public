# ## Executive summary (read this first)
# Branch-scoped Phase 2 smoke test on a real F2 practice/public-dev unit.
# It leaves the reference forecaster unchanged and checks a different panel family (G10 FX) and regime-shift card.

name: Phase 2 F2 Practice Smoke

on:
  push:
    branches: ["task/reference-docker-smoke"]
  workflow_dispatch:

permissions:
  contents: read

jobs:
  f2-practice-smoke:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683  # v4.2.2

      - name: Show immutable source revision
        run: |
          git rev-parse HEAD
          git status --short

      - name: Build unmodified reference image
        run: docker build --pull -t t2-reference:phase2-f2 .

      - name: Run real F2 unit fully offline
        run: |
          rm -rf .phase2-f2-out
          mkdir -p .phase2-f2-out
          # The image runs as uid 1000; reproduce the harness contract where /output is writable.
          chmod 0777 .phase2-f2-out
          docker run --rm --network=none \
            -v "$PWD/units/t2-F2-eur-parity-2022:/input:ro" \
            -v "$PWD/.phase2-f2-out:/output" \
            t2-reference:phase2-f2 \
            forecast \
              --panels /input/panels/ \
              --text /input/text/ \
              --asof 2022-06-30 \
              --out /output/forecast.parquet

      - name: Verify required deliverables
        run: |
          test -s .phase2-f2-out/forecast.parquet
          test -s .phase2-f2-out/forecast_meta.json
          test -s .phase2-f2-out/forecast_rationale.md
          ls -lh .phase2-f2-out
          cat .phase2-f2-out/forecast_meta.json

      - uses: actions/setup-python@0b93645e9fea7318ecaed2b359559ac225c90a2b  # v5.3.0
        with:
          python-version: "3.13"

      - name: Install pinned scorer dependencies
        run: |
          python -m pip install --upgrade pip
          pip install "qfbench2-common @ https://github.com/Agenthon-2026/Agenthon2026-public/archive/refs/tags/v2.3.1.tar.gz#subdirectory=common"
          pip install .

      - name: Inspect F2 forecast contract
        run: |
          python - <<'PY'
          import json
          import pandas as pd

          df = pd.read_parquet('.phase2-f2-out/forecast.parquet')
          meta = json.load(open('.phase2-f2-out/forecast_meta.json'))

          print(df.dtypes)
          print('rows', len(df))
          print('draws', df['draw'].nunique())
          print('assets', sorted(df['asset'].unique().tolist()))
          print('horizons', sorted(df['horizon'].unique().tolist()))
          print('value summary')
          print(df.groupby('horizon')['value'].agg(['mean', 'std', 'min', 'max']))

          assert list(df.columns) == ['draw', 'asset', 'horizon', 'value']
          assert len(df) == 500 * 1 * 1
          assert df['draw'].nunique() == 500
          assert set(df['asset']) == {'EUR'}
          assert set(df['horizon']) == {64}
          assert df['value'].notna().all()
          assert meta['unit_id'] == 't2-F2-eur-parity-2022'
          assert meta['asof'] == '2022-06-30'
          assert meta['asset_ids'] == ['EUR']
          assert meta['horizons'] == [64]
          assert meta['n_draws'] == 500
          assert meta['target'] == 'level'
          PY

      - name: Run official local admissibility gates
        run: |
          python scoring/scoring.py score \
            --card units/t2-F2-eur-parity-2022/card.toml \
            --forecast .phase2-f2-out/forecast.parquet
